{% extends 'base.html.twig' %}

{%  block title %}{{ customer.name }}{% endblock %}

{% block body %}
    <h1>{{ customer.name }}</h1>
    <ul>
        <li>{{ customer.type }}</li>
        <li>{{ customer.status }}</li>
    </ul>
{% endblock %}